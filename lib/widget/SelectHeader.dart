import 'package:flutter/material.dart';

class SelectHeader extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build

    return new ErrorWidget(null);
  }

}